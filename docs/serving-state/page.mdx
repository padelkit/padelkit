export const metadata = {
  title: "ServingState",
  description: "API reference for the ServingState dataclass representing server state.",
}

# ServingState

The `ServingState` dataclass encapsulates the serving team and player who are about to serve the upcoming/next point in the match. It is exposed through the `server` property of the [`MatchScore`](/match-score). {{ className: 'lead' }}

## Properties

<Properties>
  <Property name="team" type="TeamId">
    The [`TeamId`](/team) about to serve the upcoming/next point (`TeamId.A` or `TeamId.B`).
  </Property>
  <Property name="player" type="Player | None">
    The specific [`Player`](/player) about to serve the upcoming/next point, or `None` if player details are not configured.
  </Property>
</Properties>

---

## Initialization

<Row>
  <Col>

    Creates a new instance of a `ServingState`.

    ### Keyword Arguments

    <Properties>
      <Property name="team" type="TeamId">
        The [`TeamId`](/team) about to serve the upcoming/next point (`TeamId.A` or `TeamId.B`).
      </Property>
      <Property name="player" type="Player | None" optional default="None">
        The specific [`Player`](/player) about to serve the upcoming/next point, or `None` if player details are not configured.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Instantiation" tag="class" label="ServingState">

    ```python
    from padelkit import ServingState, TeamId, Player

    server_state = ServingState(
        team=TeamId.A,
        player=Player(name="A. Galán")
    )
    ```

    </CodeGroup>

    ```json {{ title: 'Output structure' }}
    ServingState(
      team=<TeamId.A: 1>,
      player=Player(name='A. Galán', ...)
    )
    ```

  </Col>
</Row>
